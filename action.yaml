# action.yml
name: "Auto Tag Version"
description: ""
inputs:
  version-prefix:
    description: ""
    default: "v"
  github-token:
    description: ""
    required: true
runs:
  using: docker
  image: Dockerfile
  args:
    - ${{ inputs.version-prefix }}
    - ${{ inputs.github-token }}
    - ${{ github.repository }}
